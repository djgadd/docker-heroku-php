# Heroku PHP for Docker

Something I quickly knocked together for local development against the Heroku Cedar stack with the PHP buildpack.


## Use

```
cp .env.example .env
composer install
docker-compose up
```
